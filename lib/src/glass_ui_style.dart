/// Enum specifying the active Glass UI design style.
enum GlassUIStyle {
  /// Classic static frosted glass with translucent background and fixed blur depth.
  glassmorphism,

  /// Dynamic behavior-driven glass with light refraction, cyan glow, and spring physics.
  liquidGlass,
}
